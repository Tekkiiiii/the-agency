---
name: dept-save-state
description: >
  RENAMED — use /dept-wrap instead. This skill has been renamed to better pair
  with /dept-resume. Invoke /dept-wrap [dept-slug] or /dept-wrap all.
---

# /dept-save-state — RENAMED

This skill has been renamed to `/dept-wrap`.

Use `/dept-wrap` instead. It is identical in function and pairs semantically with
`/dept-resume` (session start) and `/dept-status` (read-only check).

See `skills/dept-wrap/SKILL.md` for the full implementation.
